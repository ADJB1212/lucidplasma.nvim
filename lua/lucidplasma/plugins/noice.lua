return function(colors)
    return {
        NoiceCmdlinePopup       = { bg = 'NONE' },
        NoiceCmdlineIcon        = { fg = colors.purple },
        NoiceCmdlinePopupBorder = { fg = colors.softblue },
        NoiceMini               = { bg = colors.background_darker },
    }
end
