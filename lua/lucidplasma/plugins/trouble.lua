return function(colors)
    return {
        TroubleText   = { fg = colors.foreground_darker },
        TroubleCount  = { fg = colors.purple },
        TroubleNormal = { fg = colors.foreground },
    }
end
